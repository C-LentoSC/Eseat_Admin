import React from 'react';
import { Container, Box, Grid, Card, CardHeader, CardContent, Typography, LinearProgress, CircularProgress } from '@mui/material';
// import { Gauge, gaugeClasses } from '@mui/x-charts/Gauge';

const Dashboard = () => {

  return (
    <Container component="main" maxWidth="lg">
      <Box>
      
      </Box>
    </Container>
  );
};

export default Dashboard;